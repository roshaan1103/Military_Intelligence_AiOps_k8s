pipeline {
  agent {
    kubernetes {
      label 'jenkins-agent'
    }
  }

  stages {
    stage('Agent sanity') {
      steps {
        sh '''
          echo "Agent hostname:"
          hostname

          echo "Kubectl version:"
          kubectl version --client

          echo "Cluster access:"
          kubectl get nodes
        '''
      }
    }
  }
}

