pipeline {
  agent {
    kubernetes {
      label 'jenkins-agent'
      defaultContainer 'jnlp'
    }
  }

  stages {
    stage('Smoke') {
      steps {
        sh 'echo Connected to Kubernetes agent'
        sh 'env | grep JENKINS'
      }
    }
  }
}

