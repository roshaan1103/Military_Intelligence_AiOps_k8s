# k8s/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: aiops-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aiops-app
  template:
    metadata:
      labels:
        app: aiops-app
    spec:
      containers:
      - name: app
        image: localhost:5000/aiops-app:latest
        ports:
        - containerPort: 8080

