apiVersion: v1
kind: Service
metadata:
  name: {{ include "mi-aiops-app.fullname" . }}
  labels:
    {{- include "mi-aiops-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mi-aiops-app.selectorLabels" . | nindent 4 }}
